# 琳宝私厨 · 部署与使用说明

> 改造对象：`C:\Users\yuwen\Desktop\kitchen-main\kitchen-main`
> 改造前备份：`C:\Users\yuwen\Desktop\kitchen-main-backup`
> 技术底座：微信云开发（双人数据同步），无需自建服务器

---

## 一、首次部署（约 10 分钟，按顺序做）

### 1. 导入项目
1. 打开微信开发者工具 → 「导入项目」
2. 目录选择：`C:\Users\yuwen\Desktop\kitchen-main\kitchen-main`（注意选**内层**那个含 app.json 的目录）
3. AppID：填**你自己的小程序 AppID**（云开发必须用真实 AppID，不能用测试号；如果还没注册，先到 mp.weixin.qq.com 免费注册个人主体小程序）

### 2. 确认云开发环境
1. 顶部工具栏点「云开发」→ 若提示开通就点开通（免费额度够用）
2. 环境 ID 必须是 **cloud1-d1g65es7r8c967ed3**（app.js 里已写死）
3. 如果你的环境 ID 不一样：打开 `app.js`，把 `globalData.envId` 改成你的环境 ID

### 3. 创建数据库集合
云开发控制台 → 数据库 → 点「+」创建 4 个集合，权限保持默认即可（本项目所有读写都走云函数，不需要改权限）：
- `dishes`（菜品）
- `orders`（订单）
- `sharedCarts`（共享清单）
- `users`（用户）

### 4. 部署云函数（8 个）
在编辑器左侧找到 `cloudfunctions` 文件夹，**右键 →「创建并部署：云端安装依赖」**，逐个部署：
`login`、`user`、`manageDish`、`sharedCart`、`order`、`recommend`、`seed`、`dishMetaSync`
（新版工具支持在 cloudfunctions 根目录右键一次性全部部署）

### 5. 初始化菜品数据
云开发控制台 → 云函数 → 找到 `seed` → 「测试」运行一次
- 运行结果返回：`{ added: N, skipped: M }`
- 这会把 12 道带图菜品 + 厨房内置菜谱全部写入 `dishes` 集合（按菜名去重，重复运行不会产生重复数据）

### 5.5 同步菜品图片与营养信息（一次即可）
云开发控制台 → 云函数 → 找到 `dishMetaSync` → 「测试」运行一次
- 运行结果返回：`{ total: 59, updated: N, missing: M }`
- 作用：把 47 道内置菜谱的配图 + 全部 59 道菜的热量/功效/营养标签，按菜名写回 `dishes` 集合
- **重复运行安全**（按菜名覆盖更新，不会重复添加）

### 6. 编译运行
回到编辑器点「编译」→ 先进入身份选择页（我是客人 / 我是厨师）→ 选完自动进首页

---

## 二、双人联机测试
1. 点工具栏「预览」→ 手机微信扫码（需要开发者工具登录的微信有权限）
2. 用**两个微信账号**分别登录：
   - 账号 A 选「客人」：点菜 → 加清单 → 备注口味 → 提交订单
   - 账号 B 选「厨师」：看到「待接单」→ 接单 → 制作完成 → 记心情/拍照 → 进数据看板
3. 验证点：
   - 清单两个人实时同步（谁加的菜显示谁）
   - 订单状态流转：待接单 → 制作中 → 已完成
   - 待接单时客人可取消、厨师可拒单
   - 已完成订单自动进入「数据看板」

---

## 三、常用功能入口
| 功能 | 入口 |
| --- | --- |
| 身份切换 | 首页右上角「切换」/ 我的 → 切换身份 |
| 上传新菜（投喂官） | 我的 → 菜品管理 → 上传新菜 |
| 点菜 | 首页推荐、菜单（搜索/分类）、食材点菜 |
| 共享清单 + 提交订单 | 底部「清单」 |
| 接单/完成订单 | 底部「订单」 |
| 看菜的做法/食材/营养 | 订单里点任意一道菜 / 菜品详情页营养小卡片 |
| 数据看板 | 订单页右上角 / 我的 → 数据看板 |
| 收藏 | 菜品卡片右上角 🤍/❤️ |
| 上传新菜填热量/功效 | 我的 → 菜品管理 → 上传新菜（可选填，留空自动隐藏） |

---

## 四、发布上线（最后一步）
1. 微信开发者工具点「上传」→ 填版本号
2. 登录 mp.weixin.qq.com → 版本管理 → 提交审核 → 审核通过后点「发布」
3. 个人主体小程序：本项目无支付功能，可正常发布，无需企业资质

---

## 五、常见问题
- **报错「未找到 app.json」**：导入时目录选错了，选内层含 app.json 的 `kitchen-main` 目录
- **报错「Cloud API isn't enabled / 请先调用 wx.cloud.init()」**：检查 app.js 里 envId 是否写对、云开发是否已开通
- **菜单没菜**：`seed` 云函数没运行，或运行时报错（看云函数日志）
- **菜品没图/没营养信息**：`dishMetaSync` 没部署或没运行，运行一次即可
- **真机上没数据**：确认用的是真实 AppID 且云环境 ID 一致；模拟器预览注意切换「真机调试」
- **改了代码没生效**：改过云函数要重新部署；改过页面代码重新编译即可
